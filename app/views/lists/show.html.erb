<div class="row">
  <div class="ten columns offset-by-two">
    <ul>
      <% @list.each do |beer| %>
      <li><%= link_to beer[:name], "/beers/" + beer[:id] %></li>
      <% end %>
    </ul>
  </div>
</div>
